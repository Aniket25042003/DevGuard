# Deterministic installs: exact versions recorded in the lockfile.
save-exact=true
# Keep package trees isolated; only declared dependencies are importable.
shamefully-hoist=false
# Fail on peer-dependency mismatches instead of silently resolving them.
auto-install-peers=true
strict-peer-dependencies=false
