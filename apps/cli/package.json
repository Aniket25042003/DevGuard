{
  "name": "@devguard/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "DevGuard terminal client over /api/v1 (fetch only; no Octokit/TrueForge/policy copies).",
  "bin": {
    "devguard": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {}
}
