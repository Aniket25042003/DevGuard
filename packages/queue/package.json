{
  "name": "@devguard/queue",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Durable job queue substrate (C057): typed versioned envelopes, deterministic keys, retry classification with exponential backoff + full jitter, lease/stall semantics, cancellation fencing, DLQs. PostgreSQL stays authoritative via C008/C009-C012.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@devguard/errors": "workspace:*",
    "@devguard/logging": "workspace:*",
    "bullmq": "6.3.4",
    "ioredis": "^5.4.1"
  }
}
