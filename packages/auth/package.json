{
  "name": "@devguard/auth",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Provider-neutral authentication: principals, session lifecycle, OAuth flow ports.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@devguard/errors": "workspace:*",
    "@devguard/contracts": "workspace:*",
    "zod": "^4.1.12"
  }
}
