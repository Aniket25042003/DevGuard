{
  "name": "@devguard/config",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Typed, startup-validated runtime configuration and conservative feature flags.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@devguard/errors": "workspace:*",
    "zod": "^4.1.12"
  }
}
