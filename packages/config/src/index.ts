/**
 * @devguard/config — typed startup configuration and feature flags (C002).
 * Populated by PR-001 [C002]; this placeholder keeps the package resolvable
 * until the configuration schema lands in the same pull request.
 */
export const CONFIG_PACKAGE_VERSION = '0.0.0' as const;
