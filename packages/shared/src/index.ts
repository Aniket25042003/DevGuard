/**
 * @devguard/shared — versioned domain contracts and event schemas (C004).
 * The contract freeze lands in PR-002 [C004] after IF-1 review; this
 * placeholder keeps the package resolvable in the workspace meanwhile.
 */
export const SHARED_PACKAGE_VERSION = '0.0.0' as const;
