/**
 * @devguard/errors — provider-neutral typed error taxonomy (C003).
 * Populated by PR-001 [C003]; this placeholder keeps the package resolvable
 * until the taxonomy lands in the same pull request.
 */
export const ERRORS_PACKAGE_VERSION = '0.0.0' as const;
